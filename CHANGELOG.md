# ShotAI 更新记录

## 1.1.3 · 本地图片修改

- 图片创作并入聊天输入框，可直接生成、停止、预览和保存图片。
- 支持添加一张参考图进行本地图生图，并可调整改动幅度。
- 添加参考图后自动切换到本地图片组件，并根据原图比例选择输出形状。
- 参考图和修改结果一起保存在对话中，内网电脑也能通过主机完成图片修改。
- 图片生成与修改过程显示进度和等待动画，不影响聊天模型选择。

## 1.1.2 · GGUF 完整性检查

- 导入前读取 GGUF 版本、张量数量和模型说明数量，不再只检查文件名和前四个字符。
- 提前拦截“有张量但没有模型说明”的旧版或不完整 GGUF，避免上传数 GB 后才失败。
- 将 `llama-quantize` 校验失败改成用户可以直接理解的处理提示。
- 补充视觉双文件导入回归测试，确保不合格文件被拒绝、正常主模型和 `mmproj` 仍可一起导入。

## 1.1.1 · GGUF 导入组件修复

- 完整 CUDA 12 安装包补齐 `llama-quantize.exe` 和 `llama-server.exe`。
- 补齐对应的量化检查和推理 DLL，修复导入 GGUF 时提示辅助程序不存在的问题。
- 新增完整安装包结构门禁，缺少导入、推理、CUDA 12 或图片组件时停止发布。

## 1.1.0 · Electron Windows 客户端

- Windows 客户端切换为 Electron，提供独立窗口、任务栏图标和托盘菜单。
- Electron 主进程直接提供 9090 内网网页和本地服务转发，不再运行 `server.ps1`。
- 修复 Windows PowerShell 5.1 读取 UTF-8 中文脚本后出现乱码和语法错误的问题。
- 关闭主窗口后继续为内网提供服务，可从托盘重新打开或彻底退出。
- 自动使用已经运行、已经安装或放入数据目录的 Ollama 和图片运行组件。
- 安装程序不包含模型，卸载时默认保留模型、设置和日志。
- 修复安装后把 `ShotAI.exe` 当作 Node 子进程再次启动而出现 `ENOENT` 的问题；
  9090 服务现在直接运行在 Electron 主进程中。

## 1.1.0-preview.3 · Windows 桌面外观

- 为 `ShotAI.exe` 写入与网页一致的多尺寸 ShotAI 图标。
- 双击启动不再弹出或常驻黑色控制台窗口。
- 启动器、网页、Ollama 和图片服务的运行信息统一写入 `logs`。
- 管理员授权和启动失败提示仍然保留，确保内网端口可以开放且错误可见。

## 1.1.0-preview.2 · Windows 启动诊断与可选图片组件

- 启动网页前检查 9090 是否已经被其他程序占用，并显示可直接理解的提示。
- 将网页服务的原始输出保存到 `logs/server.log`，不再只显示 `exit status 1`。
- 网页服务提前退出或启动超时时，会在弹窗中显示日志位置和具体原因。
- 新增独立 CUDA 12 图片生成组件包；对话包继续保持不含图片组件和模型。
- 三种 Windows 发布包均不携带聊天、图片识别或图片生成模型。

## 1.1.0-preview · 轻量 EXE 启动器

- 新增 Windows x64 原生 `ShotAI.exe`，不使用 Electron。
- 新增重复启动检测、浏览器自动打开、控制端口和安全退出。
- 新增便携模型目录，启动器启动的 Ollama 固定使用 `models/ollama`。
- 新增 Ollama、图片服务、网页服务和防火墙规则的一键编排。
- 新增不含模型和大型运行组件的 EXE 精简包，预计小于 30MB。
- 新增 NVIDIA CUDA 12 对话免安装包，排除 CUDA 13、Vulkan、图片生成组件和模型权重。
- 当前预览版仍是控制台式 EXE，需要在 Windows 真机完成最终验收。

## 1.0.0 · 首个正式版本

ShotAI 1.0 把本地模型、浏览器工作台和 Windows 局域网启动器组合成一套可正式测试与交付的离线 AI 平台。

### 核心能力

- 本地流式对话，支持停止、重新回答、编辑问题、继续生成和复制。
- 自动识别文字、图片理解、回答思路和资料查找模型。
- 支持 PNG、JPEG、WebP、TXT、Markdown、PDF 与 DOCX；旧 DOC 会给出转换提示。
- 浏览器本地保存对话、资料库、偏好和最近生成的图片。
- 支持网页导入单 GGUF，以及视觉模型主文件与配套文件组合。
- 支持 stable-diffusion.cpp 本地图片创作、自动选择图片模型和网页导入单文件或三文件模型。

### 内网与交付

- 默认端口统一为 9090。
- Windows 双击启动，随包包含 Ollama v0.32.5 和图片运行组件，不要求目标电脑安装 Node.js、Ollama 或 Python；其他电脑只需浏览器。
- Ollama 与图片服务通过同一网页地址转发，修复内网访问 403、404 和图片上传失败。
- 默认只有运行 ShotAI 的主机可以添加、删除或重新载入模型；内网使用者只负责使用。
- 生产网页同时生成现代和兼容脚本，降低旧版内网浏览器黑屏或语法报错概率。

### 交互与可用性

- 主界面回归常见 AI 对话布局：对话记录、消息区域和底部输入框。
- 设置集中收纳模型、资料、回答方式、主题和本地数据清理。
- 深色与明亮主题统一颜色规则；长输入和长回答可独立滚动。
- 生成期间可浏览上方内容，完成后不再残留“正在准备回答”。
- 图片创作支持停止、进度、下载和最近记录恢复。

### 1.0 已知边界

- 不包含账号登录、HTTPS、审计日志和跨浏览器共享记录，只建议放在可信内网。
- 多人同时进行高负载任务时没有显存配额和任务队列。
- 扫描 PDF 暂无文字识别；旧 `.doc` 需要先转换为 `.docx`。
- 发布包不包含模型权重，管理员需要自行准备符合使用需求和授权要求的模型文件。
